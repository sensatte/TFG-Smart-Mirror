#:kivy 1.0
<HomeScreen>:
    ImageButton:
        id: menuButton
        keep_ratio: True
        allow_stretch: True
        source: "images/menu/app-drawer.png"
        pos_hint: {"center_x": .5, "y": 0}
        size_hint: (.17, .1)
        on_press: root.goToConfigScreen()

    InfoDayWidget:
        size_hint:(.3, .15)
        pos_hint:{"x": 0, "top": 1}

    